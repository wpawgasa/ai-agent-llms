Initialize project with CLAUDE.md. Read @docs/Codebase_Spec_LLM_Workflow_Agents_Experiment.md docs/Codebase_Spec_LLM_Workflow_Agents_Experiment.md Include project overview, implementation checklist and progress tracking. Keep CLAUDE.md not too long. Separate each module implementation detail and checklist into separate markdown files. Keep these module implementation details in `.claude/rules` directory.

---

Install github CLI

---

The repository https://github.com/wpawgasa/vlm-ocr-gpu-benchmark can be used as a reference design for several modules in this project. Indicate in CLAUDE.md that you should look for similar implementation in this repository.

---

Use /code-implementation skill to implement phase 1: Foundation.

---

Commit changes and create PR

---

Create github workflow to run tests and check coverage.

---

Use /code-implementation skill to implement phase 2: Training.

---

CI test(3.11) failed at "Run tests with coverage":

---

Action failed with error: Workflow initiated by non-human actor: claude (type: Bot). Add bot to allowed_bots list or use '*' to allow all bots.

---

CI failed at `claude-review`.

---

Use /code-implementation skill to implement phase 3: Experiment A Evaluation.

---

Some fixes were committed and caused CI to fail at test(3.11).

---

Use /code-implementation skill to implement phase 4: Quantization.

---

CI test(3.11) failed immediately after PR created.

---

Use /code-implementation skill to implement phase 5: benchmark.

---

Use /code-implementation skill to implement phase 6: Experiment C.

---

Use /code-implementation skill to implement phase 7: integration & e2e