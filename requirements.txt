# Core ML
torch>=2.4.0
# Pinned: vLLM 0.20.0's per-layer attention selector reads
# model_config.is_mm_prefix_lm (attention.py:293) and exposes
# kv_cache_dtype_skip_layers; transformers 5.6.2 ships the Gemma-4 +
# Qwen3.6 model classes our launcher hooks reference. Bumping either
# without revalidating launch_vllm_turboquant.py and vllm_plugin.py
# is a known-foot-gun.
transformers==5.6.2
peft>=0.14.0
trl>=0.15.0
accelerate>=0.34.0

# Inference
vllm==0.20.0

# Quantization kernels
triton>=3.0.0
flash-attn>=2.5.0

# Data & evaluation
datasets>=2.19.0
networkx>=3.2
jsonschema>=4.21.0

# Analysis & visualization
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Config & logging
pydantic>=2.9.0
pyyaml>=6.0
structlog>=24.0.0
wandb>=0.17.0

# Constrained decoding
outlines>=0.0.40
xgrammar>=0.1.0

# Dev
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.6.0
mypy>=1.11
