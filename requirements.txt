# Core ML
torch>=2.4.0
# vllm 0.19.0 caps transformers<5, so we declare >=4.56.0 here to satisfy the resolver.
# After install, override with transformers>=5.0 from source for Gemma 4 support:
#   uv pip install "transformers @ git+https://github.com/huggingface/transformers.git"
transformers>=4.56.0
peft>=0.14.0
trl>=0.15.0
accelerate>=0.34.0

# Inference
vllm>=0.19.0

# Quantization kernels
triton>=3.0.0
flash-attn>=2.5.0

# Data & evaluation
datasets>=2.19.0
networkx>=3.2
jsonschema>=4.21.0

# Analysis & visualization
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Config & logging
pydantic>=2.9.0
pyyaml>=6.0
structlog>=24.0.0
wandb>=0.17.0

# Constrained decoding
outlines>=0.0.40
xgrammar>=0.1.0

# Dev
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.6.0
mypy>=1.11
