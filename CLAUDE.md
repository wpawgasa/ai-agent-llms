# LLM Workflow-Orchestrating Agents with KV Cache Quantization

**Version:** 3.0 — March 2026

## Project Overview

**Benchmark-first, fine-tune-selectively** pipeline for workflow-orchestrating LLM agents on a single NVIDIA H100 SXM 80GB.

### Four-Phase Pipeline

| Phase | Scope | Output |
|-------|-------|--------|
| **Phase 1** | Benchmark 15 pre-trained candidates across 3 task categories | Rankings + 3 category winners |
| **Phase 2** | SFT then GRPO RL fine-tune only the 3 winners | 3 fine-tuned specialist models |
| **Phase 3** | KV cache quantization benchmark (models × 6 methods) | Quality/performance matrix |
| **Phase 4** | Multi-agent integration + Pareto analysis | Optimal deployment config |

### Task Categories & Model Inventory

**Category A — Prompt-Encoded Business Logic (15–35B):**
- Gemma 3 27B-IT, Qwen3-32B, Qwen3.5-35B-A3B, Qwen3.6-35B-A3B, Qwen3.6-27B, Mistral Small 3.1 24B, Nemotron-3-Nano 30B, GLM-4.7-Flash, Gemma 4 26B-A4B-IT, Gemma 4 31B-IT

**Category B–C — Specialist Subagent & Graph Extraction (2–5B):**
- Qwen2.5-3B-Instruct, Qwen3.5-4B, GLM-4.7-Flash, Gemma-2B, Gemma-3-4B-it, Gemma-4-E4B-it, Gemma-4-E2B-it

## Reference Repository

[wpawgasa/vlm-ocr-gpu-benchmark](https://github.com/wpawgasa/vlm-ocr-gpu-benchmark) serves as a reference design for this project.

## Tech Stack

- Python, PyTorch >=2.4, Triton >=3.0, vLLM >=0.8 (PagedAttention v2)
- **Unsloth >=2025.3** — SFT + GRPO RL (2x faster, 70% less VRAM, MoE kernels)
- HF Transformers >=5.0, PEFT >=0.14, TRL >=0.15 (GRPOTrainer)
- W&B for logging, Outlines/XGrammar for constrained decoding
- Target: Single NVIDIA H100 SXM 80GB

## Repository Structure

```
configs/          - YAML configs (models/cat_a/, models/cat_bc/, training/, quantization/, benchmark/, serving/)
data/             - Data generation and templates (output: task_a/, task_b/, task_c/)
training/         - Phase 2: SFT + GRPO RL (sft.py, grpo.py, rewards/, lora_targets.py, pilot_check.py)
quantization/     - Phase 3: KV cache quantization (turboquant/, rotorquant/, baselines/)
eval/             - Evaluation modules (state accuracy, tool F1, graph, PPL, composite_score.py, quant_benchmark.py)
integration/      - Phase 4: multi-agent orchestrator, E2E benchmark, Pareto (orchestrator.py, pareto.py)
serving/          - vLLM serving utilities (launch_vllm.sh, vllm_utils.py)
analysis/         - Visualization (plot_phase1_rankings.py, plot_sft_vs_rl.py, plot_quant_matrix.py, plot_pareto.py)
scripts/          - Experiment runners (run_phase1.sh, run_phase2_sft.sh, run_phase2_grpo.sh, run_phase3.sh, run_phase4.sh)
tests/            - Unit and integration tests
```

## Module Implementation Details

Detailed specs for each module are in `.claude/rules/`:
- `01-configs.md` - Configuration schema and YAML files
- `02-data-generation.md` - Data generation layer (Tasks A, B, C)
- `03-training.md` - SFT + GRPO RL training (Unsloth, reward functions)
- `04-quantization.md` - KV cache quantization (TurboQuant, RotorQuant, baselines)
- `05-eval.md` - Evaluation metrics and benchmarks
- `06-serving.md` - vLLM serving utilities
- `07-analysis.md` - Result analysis and visualization
- `08-testing.md` - Testing strategy
- `10-integration.md` - Phase 4 multi-agent integration and Pareto

Phase 1 benchmarking runs through `eval/agent_benchmark.py` (invoked by `scripts/run_exp_a*.sh`). The separate `benchmark/` package was removed; see git history.

## Implementation Progress

### Phase 1: Foundation — Data & Configs
- [x] Project scaffolding (pyproject.toml, requirements.txt, directory structure)
- [x] Configuration schema and YAML files (`configs/`)
- [x] Add Gemma 4 model YAML configs (`configs/models/cat_a/gemma4_26b_a4b.yaml`, `gemma4_31b.yaml`; `configs/models/cat_bc/gemma4_e4b.yaml`, `gemma4_e2b.yaml`)
- [x] Add Qwen3.6 model YAML configs (`configs/models/cat_a/qwen36_35b_a3b.yaml`, `qwen36_35b_a3b_fp8.yaml`, `qwen36_27b.yaml`, `qwen36_27b_fp8.yaml`)
- [x] Data generation module (`data/`)
- [x] Data validation and chat template converter

### Phase 2: Training
- [x] LoRA target module registry (`training/lora_targets.py`) — 10 models incl. qwen35_35b_a3b, nemotron_30b
- [x] Add Gemma 4 model entries to `training/lora_targets.py` (gemma4_26b_a4b, gemma4_31b, gemma4_e4b, gemma4_e2b)
- [x] Add Qwen3.6 model entries to `training/lora_targets.py` (qwen36_35b_a3b, qwen36_27b)
- [x] Unified SFTTrainer entry point (`training/train_specialist.py` — v2 backward-compat)
- [x] Graph extraction trainer (`training/train_graph_extractor.py` — v2 backward-compat)
- [x] Adapter merge utility (`training/merge_adapter.py`) — with quantize_merged param
- [x] Unsloth SFT entry point (`training/sft.py`)
- [x] Unsloth GRPO RL entry point (`training/grpo.py`)
- [x] Task-specific reward functions (`training/rewards/`)
- [x] Shared reward helpers (`training/reward_utils.py`)
- [x] Pilot check for fine-tuning response (`training/pilot_check.py`)

### Phase 3: Experiment A Evaluation
- [x] State machine accuracy eval (`eval/state_accuracy.py`)
- [x] Tool-call F1 eval (`eval/tool_call_f1.py`)
- [x] Tool chain propagation eval (`eval/tool_chain_propagation.py`)
- [x] Agent benchmark composite (`eval/agent_benchmark.py`)
- [x] vLLM launch script (`serving/launch_vllm.sh`)
- [x] Composite score module (`eval/composite_score.py`)
- [x] Quantization benchmark harness (`eval/quant_benchmark.py`)

### Phase 4: Quantization (KV Cache)
- [x] TurboQuant codebook + rotation (`quantization/turboquant/`)
- [x] TurboQuant Triton kernels + vLLM integration
- [x] RotorQuant Clifford algebra + Triton kernels (`quantization/rotorquant/`)
- [x] Baseline wrappers: KIVI, KVQuant (`quantization/baselines/`)

### Phase 5: Quantization Benchmarks
- [x] Perplexity eval (`eval/perplexity.py`)
- [x] LongBench eval (`eval/longbench.py`)
- [x] Needle-in-a-Haystack eval (`eval/needle_haystack.py`)
- [x] Benchmark matrix execution (10 models x 6 methods)

### Phase 6: Graph Extraction Eval
- [x] Graph extraction eval (`eval/graph_extraction_eval.py`)
- [x] Constrained decoding integration (Outlines/XGrammar)

### Phase 7 (v2) / Phase 4 (v3): Integration & E2E
- [x] Multi-agent orchestrator (`serving/orchestrator.py` → v3: `integration/orchestrator.py`)
- [x] E2E benchmark (`serving/benchmark_e2e.py` → v3: `integration/benchmark_e2e.py`)
- [x] Pareto frontier analysis (`analysis/pareto.py` → v3: `integration/pareto.py`)
- [x] Visualization (`analysis/plot_results.py` → v3: split into `analysis/plot_*.py`)
- [x] Experiment runner scripts
- [x] v3 integration module (`integration/`)
- [x] v3 analysis plots (plot_phase1_rankings.py, plot_sft_vs_rl.py, plot_quant_matrix.py, plot_pareto.py)

## Key Architecture Decisions

- **ADR-001**: Benchmark-first model selection (evaluate all 15 pre-trained first, fine-tune only 3 winners)
- **ADR-002**: Unsloth over standard PEFT/TRL (2x speed, 70% less VRAM, GRPO + MoE kernels)
- **ADR-003**: SFT then GRPO two-stage training (SFT for format/domain, GRPO for task metric optimization)
- **ADR-004**: Shared SFT base for dual-category winners (diverge at GRPO stage)
- **ADR-005**: Triton for custom quantization kernels (TurboQuant, RotorQuant)
- **ADR-006**: Use upstream vLLM TurboQuant variants (`turboquant_3bit_nc`, `turboquant_4bit_nc`, `turboquant_k3v4_nc`, `turboquant_k8v4`) for Phase 3 benchmarks instead of the project's custom `"turboquant"` string. vLLM v1's plugin architecture requires a full `AttentionBackend` subclass for any new KV compression scheme; monkey-patching module-level cache I/O (the v0 approach) no longer works. Upstream ships `vllm.v1.attention.backends.turboquant_attn.TurboQuantAttentionBackend`, so we adopt it directly.

## Pending Work: Custom KV Cache Backends

The project's custom TurboQuant/RotorQuant code (`src/llm_workflow_agents/quantization/{turboquant,rotorquant}/`) is **scaffolding-only** against current vLLM. Launchers (`src/llm_workflow_agents/serving/launch_vllm_{turboquant,rotorquant}.py`) wire plain `"turboquant"` / `"rotorquant"` through argparse and a Pydantic validator wrapper, so `CacheConfig` accepts them and the server starts — but no compression happens because stock attention backends ignore the dtype string.

To make the custom implementations actually compress KV cache at inference time, both would need a full v1 `AttentionBackend` subclass (reference: `vllm.v1.attention.backends.turboquant_attn.TurboQuantAttentionBackend`, ~800 LOC). Each port must implement `get_kv_cache_shape`, `supported_kv_cache_dtypes`, `AttentionImpl.forward` (prefill + decode), and `AttentionMetadataBuilder`; register via `register_backend(AttentionBackendEnum.CUSTOM, "...")` and monkey-patch `STR_DTYPE_TO_TORCH_DTYPE` in the launcher for the new dtype string.

**Decision (2026-04-21): neither port will be written for Phase 3.**

- **TurboQuant (project variant):** not porting. The only thing the project's version has that upstream's four variants don't is the QJL residual path — a ~1-2 PPL optimization at best. Cost (2-3 days port + 2-3 days validation) is not justified when upstream's `turboquant_3bit_nc` / `_4bit_nc` / `_k3v4_nc` / `_k8v4` already cover the compression/quality curve. Use upstream.
- **RotorQuant:** porting gated on a standalone microbenchmark. Before investing 1-2 weeks on a v1 backend, validate the Cl(3,0)-rotor-vs-Hadamard quality claim with a PyTorch-eager microbenchmark on Qwen2.5-3B held-out KV tensors. Compare reconstruction error + downstream PPL against Hadamard rotation at matched bit budget. If RotorQuant wins materially, port. If not, drop it from Phase 3 and the research track.
- **Scaffolding (`launch_vllm_turboquant.py` project-custom path, `launch_vllm_rotorquant.py`, `register_turboquant_backend` Pydantic patch):** keep in-tree but route only exact `"turboquant"` / `"rotorquant"` strings through the custom launchers (`serving/launch_vllm.sh:148-154`). Upstream `turboquant_*` variants bypass the project path entirely and go straight to stock vLLM.
- **`_patch_block_size` / `_patch_paged_attention` in `vllm_integration.py`:** leave as no-op with migration warnings until a port actually lands (or until RotorQuant is dropped, whichever comes first).

Phase 3 benchmark matrix: upstream `turboquant_*` variants + FP8 + KIVI + KVQuant + AWQ-INT4+FP8 KV. RotorQuant column is **provisionally deferred** pending the microbenchmark result.

### KV Cache CPU Offload (LMCache) — serving knob, not a benchmark target

The docker serving stack (`deployments/models/docker-compose.yml` + `.l4.yml`) exposes an opt-in LMCache CPU KV-cache offload knob via `VLLM_LMCACHE_ENABLED` (+ `VLLM_LMCACHE_CPU_SIZE`/`_CHUNK_SIZE`/`_VERSION`/`_KV_TRANSFER_CONFIG`; see `.env.example`). When enabled, the entrypoint pip-installs `lmcache` at container start and passes `--kv-transfer-config` to vLLM, offloading **full-precision** KV to CPU RAM and re-fetching on prefix reuse. This is **orthogonal to and not stackable with** the on-GPU KV-quant backends: combining `VLLM_LMCACHE_ENABLED` with `VLLM_KV_CACHE_DTYPE=turboquant*`/`rotorquant*` hard-fails in the entrypoint (both hook KV cache I/O). `fp8` KV is *not* blocked and is **smoke-tested working** with LMCache (2026-07-06: gemma-4-12B-it FP8-Dynamic on vLLM 0.24.0 / LMCache 0.5.0 — 1280-token prefix stored to CPU at ~10.6 GB/s, hit on reuse, generations correct); full-precision CPU buffer alongside an fp8 on-GPU cache round-trips fine in that pairing, but it has not been validated on the 0.20.0 image. It is a serving/deployment knob only — not a Phase 3 quantization benchmark column. Entrypoint logic is unit-tested via `tests/test_lmcache_entrypoint.py` using the `VLLM_ENTRYPOINT_DRY_RUN` affordance (no GPU/docker required).

**LMCache ⊥ hybrid models (verified 2026-07-02).** LMCache is **hard-broken with any hybrid (sliding+full attention or Mamba) model on vLLM 0.20.0** — this covers **all Gemma-4 variants (E2B/E4B/26B-A4B/31B)** and **Nemotron**. `LMCacheConnectorV1` does not implement `SupportsHMA`, so its `--kv-transfer-config` forces vLLM's hybrid KV cache manager *off*; but Gemma-4's mixed sliding/full attention groups *require* it. There is no working flag combination — both branches crash `EngineCore` at startup during KV-cache init:
- **Default** (hybrid manager off): `KeyError: '…self_attn.attn'` in `get_attn_backends_for_group` (`gpu_model_runner.py:6265`) — layers of one group aren't in that group's `kv_cache_specs`. Fires in *both* the CUDA-graph profiler path (`profile_cudagraph_memory`) and the real `initialize_from_config` path, so `--enforce-eager` does **not** help (it only gates the former).
- **Force it on** (`--no-disable-hybrid-kv-cache-manager` via `VLLM_EXTRA_ARGS`): `ValueError: Connector LMCacheConnectorV1 does not support HMA but HMA is enabled`.

`enforce_eager` and `fp8` KV are red herrings here — the blocker is architectural. **Only enable LMCache for non-hybrid dense models** (e.g. Qwen3.6 dense variants). Consider a future entrypoint guard that hard-fails `VLLM_LMCACHE_ENABLED` + a known-hybrid model, mirroring the existing turboquant/rotorquant guard, so it fails fast with a clear message instead of the cryptic `KeyError`.

**Update (2026-07-06): the incompatibility is v0.20.0-specific, resolved on newer images.** vLLM 0.24.0 + LMCache 0.5.0 serves `gemma-4-12B-it` FP8-Dynamic (`gemma4_unified`, hybrid sliding/full) with LMCache on the L4 — prefix store + hit verified, generations correct (run used `--enforce-eager`; graph mode untried in this pairing). So the guard above applies to the pinned v0.20.0 default image; hybrid models can use LMCache by moving to `VLLM_IMAGE=vllm/vllm-openai:v0.24.0` (see Risk R11 / `.env.example` L4 preset). Two additional data points: (1) on E-series the first missing key in the 0.20.0 `KeyError` is a KV-shared layer (E4B: 42 layers, `num_kv_shared_layers: 18` → `layers.24` is the first of 18 layers owning no KV spec); (2) even on images where it boots, E-series is a poor LMCache fit — with the hybrid manager off, its sliding-window layers (window 512, the majority) hold KV for the full context, inflating on-GPU KV usage.

### Known Model × TurboQuant Incompatibilities

| Model | Reason | Workaround |
|-------|--------|------------|
| Gemma4 26B-A4B, Gemma4 31B | vLLM v1's KV cache profiler (`gpu_model_runner.py:6598`) fails to `.view()` padded raw tensors across mixed KV cache groups when the sliding:full layer ratio requires padding. | Auto-resolved. `_install_turboquant_engine_config_hook` in `launch_vllm_turboquant.py` detects Gemma-4 via `AutoConfig.architectures` and auto-injects `enforce_eager=True`, which gates out `profile_cudagraph_memory` (`gpu_worker.py:380-385`). Decode throughput drops ~15-25% (no CUDA graphs); numerical correctness unaffected. Restore graph-mode once upstream lands a per-layer-aware profiler. |
| Nemotron-3-Nano 30B (Mamba hybrid) | `arg_utils.py:1649` raises `NotImplementedError` on any hybrid + `turboquant_*`. Also blocked by separate Mamba+vLLM compat issues (Risk R6). | Use HF `generate()` fallback path; TurboQuant not viable here. |

Compatible targets for the TurboQuant cells: Qwen3-32B, Qwen3.5-35B-A3B, Qwen3.6-35B-A3B (+ FP8), Qwen3.6-27B (+ FP8), Mistral-Small-3.1-24B, Gemma-3-27B, GLM-4.7-Flash, Gemma4-26B-A4B, Gemma4-31B. Qwen3.5/3.6 unblocked via `_install_turboquant_engine_config_hook` which masks `ModelConfig.is_hybrid` to False during `create_engine_config` — DeltaNet/Mamba layer indices in the boundary-skip list are harmless no-ops since those layers don't construct `Attention()` modules. Gemma-4 unblocked as described in the table.

## Known Risks

- R1: Qwen3.5-35B-A3B BF16 ~70GB → Unsloth QLoRA 4-bit (~17.5GB) + FP8 RL
- R2: Same model wins Cat B + Cat C → share SFT base, diverge at GRPO
- R3: Phase 1 winner doesn't respond to fine-tuning → 100-step pilot SFT on top-2 (`training/pilot_check.py`)
- R4: TurboQuant PR not merged → use `0xSero/turboquant` fork
- R5: GRPO reward hacking → held-out eval every 50 steps, KL monitoring, auto-stop
- R6: Nemotron Mamba + vLLM incompatibility → HF `generate()` fallback. Qwen3.5/3.6 hybrid (DeltaNet) unblocked for `turboquant_*` via `_install_turboquant_engine_config_hook` in `launch_vllm_turboquant.py`; Nemotron-3-Nano remains out of scope (Mamba layers + vLLM compat issues beyond the hybrid guard).
- R7: GLM LoRA VRAM overflow → auto-reduce rank 64→32 or inference-only
- R8: Gemma4 + TurboQuant → **works with perf caveat**. Two hooks in `launch_vllm_turboquant.py`: (1) `_install_gemma4_mixed_backend_hook` bypasses the TRITON_ATTN global force at `vllm/model_executor/models/config.py:100`; (2) `_install_turboquant_engine_config_hook` auto-injects `enforce_eager=True` for Gemma-4 + `turboquant_*`, skipping vLLM v1's broken mixed-KV profiler (`gpu_model_runner.py:6598`). CUDA graphs off → expect ~15-25% decode throughput penalty vs graph-mode. Numerical correctness unaffected. Restore graph-mode once upstream lands a per-layer-aware profiler.
- R9: Gemma-4 (26B-A4B, 31B) + Unsloth `fast_inference=True` → **architecture allowlist rejection**. Unsloth 2026.5.2's `VLLM_SUPPORTED_VLM` at `unsloth/models/vision.py:242` is hardcoded to `{qwen2_5_vl, gemma3, mistral3, qwen3_vl, qwen3_vl_moe}`. Gemma-4 ships as a SigLIP+Gemma4 multimodal stack, so `FastLanguageModel.from_pretrained(..., fast_inference=True)` raises `RuntimeError: Found architectures: siglip, gemma4!` (`unsloth/models/vision.py:610`). **Auto-resolved.** `training/grpo.py::_detect_model_family` reads the SFT checkpoint's `adapter_config.json → base_model_name_or_path` and resolves `AutoConfig.model_type`. When the family is in `UNSLOTH_VLLM_INCOMPATIBLE_FAMILIES = {"gemma4"}`, `use_vllm` is forced to `False` with a `vllm_rollout_disabled_unsloth_incompat` warning; training proceeds with HF `model.generate()` rollouts (~31 s/step on H100 80GB → ~8.6 h per 1000-step GRPO run). Other Cat A models (Qwen3-32B, Qwen3.5-35B-A3B, Mistral-Small-3.1-24B, Qwen3.6 variants) are unaffected — they continue to get Unsloth colocate vLLM rollouts. To re-enable vLLM rollouts on Gemma-4: (a) wait for Unsloth to add `gemma4` to the allowlist (file an issue at github.com/unslothai/unsloth), (b) switch to external vLLM server mode (`vllm_mode="server"` + a separate `trl vllm-serve` process — tight on 80GB but viable with FP8 weights), or (c) monkey-patch `unsloth.models.vision.VLLM_SUPPORTED_VLM` at import time (quick to try, may fail deeper in vLLM-engine init).
- R10: DiffusionGemma 26B-A4B + vLLM → **no tagged release supports it** (verified 2026-06-11: vLLM 0.22.1 model registry has no `diffusion_gemma` entry; transformers added the model defs in 5.11.0; the HF checkpoints ship no remote code). Serving works ONLY via vLLM's `dgemma` branch (PR #45163, open) — prebuilt images `vllm/vllm-openai:gemma-cu129` (CUDA 12.9) / `:gemma` (CUDA 13.0, driver ≥ 580). Required flags per the RedHatAI model card: `VLLM_USE_V2_MODEL_RUNNER=1`, `--trust-remote-code`, `--max-num-seqs 4`, `--hf-overrides '{"diffusion_sampler": "entropy_bound", "diffusion_entropy_bound": 0.1}'`. Wired into `deployments/models/` (`VLLM_IMAGE` + `VLLM_USE_V2_MODEL_RUNNER` + `VLLM_TRUST_REMOTE_CODE` knobs; preset in `.env.example`). `serving/launch_vllm.sh`'s `.venv-infer` (vLLM 0.20.0) cannot serve it — fails at ModelConfig validation ("model type `diffusion_gemma` ... not recognize this architecture"). Keep `kv-cache-dtype` blank (fp8 KV on the diffusion decode path unvalidated). Re-pin to a tagged image once PR #45163 merges into a release.
- R11: Gemma-4 12B (`gemma4_unified`) + vLLM → **needs vLLM ≥ v0.23.0** (verified 2026-07-06 against the model registry: `Gemma4Unified*` absent through v0.22.1, present in v0.23.0/v0.24.0). The pinned serving image (`vllm/vllm-openai:v0.20.0`) and `serving/launch_vllm.sh`'s `.venv-infer` (vLLM 0.20.0) both fail at ModelConfig validation ("model type `gemma4_unified` ... does not recognize this architecture"). On L4 24GB the BF16 checkpoint can never fit regardless (11.96B params ≈ 23.9 GB weights > 22.03 GiB usable) — use `RedHatAI/gemma-4-12B-it-FP8-Dynamic` (~12.75 GB load, compressed-tensors FP8, tested by RedHat against vLLM nightly) with `VLLM_IMAGE=vllm/vllm-openai:v0.24.0`. Verified serving on L4 (2026-07-06): fp8 KV, `--max-num-batched-tokens 4096`, 50,087-token KV cache at 8K max_model_len; tool calling via parser `gemma4` works; also verified with LMCache + `--enforce-eager`. Preset in `.env.example`.
- R12: Cat A SFT corpus role corruption — messages with malformed roles like `assistant to=tool` / `assistant to=book_reservation` and empty `content` (likely leaked teacher-model tool-routing syntax that failed to parse into `<tool_call>` blocks), present at every stage of the Task A pipeline: `data/output/sft/task_a_splits` (canonical, DVC-tracked: 5.1% train / 5.7% validation / 4.6% test conversations affected) and worse in `data/output/grpo/task_a` (L3-L5 filtered, what the held-out audit draws from: 17.2% train / **22.4% validation**). 62.9% occur immediately after a `tool`-role message. Root cause pinned to `scripts/clean_task_a_sft.py::clean_record`'s `_is_role_confused()`, which only strips `tool`-role messages containing literal `<tool_call>` text — a different bug — and has no logic for a corrupted `role` field itself; `scripts/split_task_a_sft.py` and `scripts/filter_grpo_data.py` don't validate roles either, so it passes through every stage untouched. `data/data_validator.py` only validates that `messages[0]` is role `system`. Gemma-4's chat template does not reject unknown roles — it renders them literally (`chat_template.jinja:219`), so under `loss_mask: all_tokens` the model trains with full gradient weight on these degenerate empty turns. Discovered 2026-07-14 during forensics on the "announce-but-don't-call" tool-emission gap (see `docs/grpo_tool_emission_gap_review.md` §5.1). **Fixed (2026-07-14):** `clean_task_a_sft.py::clean_record` now drops any message whose role isn't in `{system, user, assistant, tool}` (TDD: 4 new tests added to `tests/unit/test_clean_task_a_sft.py`, 12/12 pass, no regressions across 167 related unit tests). Regenerating uncovered a **separate drift bug**: the current raw `data/output/sft/task_a` (5 consolidated "merged" files, 5,549 convs) had already been replaced/consolidated at some point and carries **zero** instances of this corruption — but `task_a_cleaned`/`task_a_splits`/`data/output/grpo/task_a` had never been regenerated from it, so they stayed stale and corrupted regardless of the raw fix. `dvc.lock` confirms the drift: its recorded output hash for `task_a_cleaned` from the `task_a_sft` stage didn't match its recorded dependency hash for the same path in `task_a_sft_splits` — two different stages disagreeing about the same directory's contents. Regenerated all three (`clean_task_a_sft.py` → `split_task_a_sft.py --force` → `filter_grpo_data.py`) from current raw data with the patched cleaner; verified 0/5,549 (`task_a_cleaned`), 0/5,549 (`task_a_splits`), 0/2,853 (`grpo/task_a`) malformed-role conversations post-regen. New split sizes: train 4,716 / validation 554 / test 279 (up from 4,414/519/261 — the corpus grew since the stale directories were last built). Old (corrupted) directories preserved as `*.pre_r12fix_backup_20260714T104107Z` for rollback/comparison, not deleted. **Follow-up still needed:** `dvc.lock` is stale again (no `dvc` CLI in this environment to run `dvc commit`/`dvc push`) — run those on a machine with DVC configured so the lock file and GCS remote reflect this regeneration.
- R13: `.runs/sft_cat_a/sft_cat_a.yaml` provenance bug — the frozen config recorded for checkpoint-1000 claimed `data.source: data/output/sft/task_a_cleaned/splits` (an untracked, non-canonical, half-sized snapshot; not a declared DVC output), but `checkpoints/sft_cat_a/gemma-4-26B-A4B-it/train.log` proves the actual run loaded 9,131 train / 1,074 eval examples — an exact match for the canonical, DVC-tracked `data/output/sft/task_a_splits`, not the 4,414/519 the frozen config implied. So checkpoint-1000 **was** trained on the correct canonical dataset. **Root cause found:** `scripts/run_phase2_sft.sh`'s patched-config output path (`PATCHED_CFG="$PROJECT_ROOT/.runs/sft_cat_a/sft_cat_a.yaml"`) is fixed/non-run-specific — every invocation of the script, including `--dry-run`, silently overwrites it with whatever `configs/training/sft_cat_a.yaml` says *at that later invocation*. The file we found was almost certainly clobbered by a later invocation (base config had since been repointed at the stale directory), not a faithful record of the July 2 run that produced ckpt-1000. **Fixed (2026-07-14):** corrected the stale field back to `data/output/sft/task_a_splits`, matching what `train.log` proves was actually used. **Fully fixed (2026-07-14):** `scripts/run_phase2_sft.sh` now computes `RUN_TS` (UTC timestamp) once per invocation, names the patched config `sft_cat_a_${RUN_TS}.yaml` under `.runs/sft_cat_a/` (never overwritten by a later run), and additionally copies it to `$CKPT_DIR/frozen_sft_config_${RUN_TS}.yaml` — co-located with `train.log` so per-run provenance doesn't require knowing about `.runs/` at all. Verified by simulating the exact bash+python logic in isolation (no `.venv-train`/unsloth needed for this part): resolved `data.source` renders correctly, two runs in the same session produce distinct files, no clobbering. `scripts/run_phase2_grpo.sh` had the **identical** fixed-path bug (`PATCHED_CFG="$PATCHED_DIR/${GRPO_STEM}.yaml"`) — **fixed 2026-08-16, see R17** (`PATCHED_CFG` now `RUN_TS`-stamped, checkpoint path pinned via an explicit `output_dir`). **Follow-on defect this fix introduced, found and fixed 2026-07-22:** `sft.py` derived the checkpoint directory from `Path(config_path).stem`, so the now-timestamped patched config silently moved the output path too. The 2026-07-22 C0 retrain wrote all 1,770 steps of weights to `checkpoints/sft_cat_a_20260722T051246Z/` while the DVC stage's declared out `checkpoints/sft_cat_a/gemma-4-26B-A4B-it` captured only `train.log` + the frozen config (208 KB, no adapters) — and `dvc repro` then recorded that weightless directory as the stage output. The run itself was fine (best eval_loss 0.1798, no overfit); only the path was wrong, and the weights were relocated. Fixed in `2ba5a70` via `training/sft.py::_resolve_output_dir()`: an explicit `output_dir` config key wins, else the config stem with a trailing `_YYYYMMDDTHHMMSSZ` run stamp stripped — provenance stays in the config filename, the checkpoint path stays stable. `run_phase2_sft.sh` now writes `output_dir` into the patched config and reads it back into `CKPT_DIR` so the shell and `sft.py` resolve from one source (they had disagreed inside a single run's own banner). Regression cover: `tests/unit/test_sft_output_dir.py` (6 tests). **The explicit `output_dir` key is also the supported way to give each factorial cell its own checkpoint directory** — without it, a C1 run overwrites C0.
- R14: Cat A SFT token budget — **`max_seq_length: 4096` right-truncated 56% of training conversations, and `loss_mask: all_tokens` spends 79% of gradient on tokens the model never emits.** Measured 2026-08-04 on `data/output/sft/task_a_splits/train.jsonl` (250 conversations, system message rebuilt via `build_enriched_system_prompt` as training does); reproduce with `scripts/measure_sft_token_budget.py`. Per-role share: **system 71.4% / assistant 21.2% / user 4.9% / tool 2.5%** — the enriched contract alone is a median 3,016 tokens, largely byte-identical across all 4,716 examples. So `response_only` is a **4.72× gradient-density increase** on assistant tokens at equal compute, and the 2.5% tool span under `all_tokens` actively trains the model to *generate* `{"status": "success", ...}` payloads (the hallucination R12/the tool-emission-gap work exists to suppress). Separately and more seriously: conversation length is median **4,264** tokens / p90 6,055 / max 9,108, so **139/250 (56%) exceed the 4096 window** used by the run that produced weights (`checkpoints/sft_cat_a/gemma-4-26B-A4B-it/frozen_sft_config_20260722T051246Z.yaml`). `training/sft.py:674-675` passes `truncation=True, max_length=…` with `truncation_side='right'`, so those rows lost their **ending** — the terminal `[STATE: X → TERMINAL]` transition and the final tool calls — which is exactly the region the low terminal-state / task-completion metrics measure. 8192 covers 99% of conversations, 16384 all of them. **Consequence:** ckpt-500, ckpt-1770 and the pre-registered ≥0.75 composite bar were all measured against a partly truncated training set; treat them as a floor, not a clean comparison. **Note the render paths truncate in opposite directions** — `render_response_only_sample` left-truncates by design (`sft.py:37-38`, "so the final assistant turn always survives") while `all_tokens` right-truncates, so any A/B of the two recipes at 4096 confounds the loss mask with the truncation direction. Nothing changed in config yet (both remain `all_tokens` / 4096 so cell C0 reproduces the baseline); the recommended three-cell design — C0 `all_tokens`@4096, C1 `all_tokens`@8192 to isolate truncation, C2 `response_only`@8192 — plus why `eval_loss` cannot rank the cells is in `docs/cat_a_loss_mask_and_truncation_analysis.md`. Use an explicit `output_dir` per cell (see R13).
- R15: **[VERDICT SUPERSEDED 2026-08-16 — see R17. The corpus is not bad; this comparison measured the R16 window bug, not the corpus.]** task-a-sft-v2 corpus **regresses held-out performance — do not run GRPO on `checkpoint-1767` (`sft-gemma4-v4`)**. The stay-convention remediation (tag `task-a-sft-v2`, R12 follow-on) hit every acceptance gate it set — 0 stay violations, 20,340/20,340 tool turns self-looping — and the C0 retrain on it improved `eval_loss` to **0.17148** vs v3's 0.17978. But on **206 conversations held out from both lineages** (greedy, seed 42, GT in the v2 convention, eval prompt stating the stay rule to both, contamination-controlled — 63 of v2's 278 test conversations are in v3's *training* set, matched on a user-turn fingerprint because `conversation_id` is **not** unique), the composite went **0.5709 → 0.5120**: paired-bootstrap 95% CI **[−0.0913, −0.0278]**, sign test **p = 7.7e-4** (50 v3-better vs 21 v4-better of 71 discordant rows). All four components regressed. **Mechanism:** the remediation moved *every* tool call onto a self-loop turn, taking **P(tool call | advancing annotation) from 10.7% (3,710/34,739) to 0.0% (0/34,664)**. The model emits the state annotation *first* and is right only ~39.8% of the time on self-loop turns; on the other ~60% there is now no path back to a tool call, where v1 left a 10.7% escape hatch. Consequences on the held-out set: "emits NO tool call" on tool-bearing rows **35.2% → 57.7%**, `tool_f1` there **0.366 → 0.249**, and spurious self-loops on *advancing* rows **3.7% → 26.9%** — the convention generalised as an unconditional habit, not one conditioned on tool-calling. Aggregate tool-call density barely moved (31.3% → 30.3% of assistant turns), so density cannot explain it; the *conditional structure* can. This makes the "announce-but-don't-call" gap **worse** and adds fabricated tool results (a row narrates performing the call and reports its outcome without emitting one). **`eval_loss` moved opposite to capability** because each run scores its own validation split and a remediated corpus is a more predictable one — same reason `eval_loss` cannot rank the C0/C1/C2 cells (R14). **Next steps, in order:** (1) run **C2 (`response_only` @ 8192)** on the v2 corpus with an explicit `output_dir` (R13) before blaming the corpus — C0's 79%-wasted gradient and right-truncation of 56% of rows (R14) may simply leave the convention under-trained; (2) consider relaxing the 0.0% so a tool call can still follow an advancing annotation — the convention should constrain the *annotation*, not forbid co-occurrence; (3) `data/output/grpo/task_a` is still v1-derived and must be repro'd from whichever corpus wins before any GRPO run. Full analysis, evidence and repro: `docs/cat_a_corpus_v2_heldout_regression.md`, `runs/audit/heldout_ckpt{1767,1770}_*.json`, and the `sft-gemma4-v4` tag annotation. **v3 (`checkpoint-1770`) remains the better checkpoint on held-out**; both lineages share `checkpoints/sft_cat_a/gemma-4-26B-A4B-it` and both contain `checkpoint-500/-1000/-1500` with different weights, so materialize out-of-place to compare (versioning doc §4). **Resolution (2026-08-16):** step (1) was executed — C2 (`response_only` @ a *real* 8192 window, post-R16-fix) on the v2 corpus scores **0.7595** on this same 206-row set, +0.1887 over v3 with 95% CI [+0.1505, +0.2288]. Every failure mode catalogued above reversed: "emits NO tool call" 57.7% → 12.7%, `tool_f1` on tool-bearing rows 0.249 → 0.636, spurious self-loops on advancing rows 26.9% → 2.8%. The v2 convention is simply unlearnable from a 1024-token window — v3 and v4 never saw a tool-calling turn. **`checkpoint-1767` of C2 (`checkpoints/sft_cat_a_c2/`, not the v4 checkpoint of the same number) supersedes v3 as the best Cat A checkpoint.** Step (2) (relaxing P(tool call | advancing) from 0.0%) is no longer indicated — C2 learned the rule as a conditional without it. Step (3) still stands. See R17 and `docs/cat_a_c2_heldout_result.md`.
- R16: **Every Cat A SFT run before 2026-08-13 trained on a 1024-token window** — `configs/training/*.yaml`'s `max_seq_length` has been **inert for training** since the TRL upgrade. TRL's `SFTTrainer` builds `DataCollatorForLanguageModeling(max_length=args.max_length, truncation_mode=args.truncation_mode)` (`trl/trainer/sft_trainer.py:891`) and re-truncates every batch (`:183-197`) regardless of the pre-tokenized lengths `_render_chat` produces; `SFTConfig.max_length` defaults to **1024** with `truncation_mode='keep_start'`. `sft.py` passed the ceiling only `if "max_seq_length" in inspect.signature(SFTConfig).parameters` — but TRL renamed that kwarg to `max_length` in 0.23+, so on TRL 1.0.0 the branch was a silent no-op. Surfaced 2026-08-13 when cell C2 (`response_only` @ 8192) logged `loss=0, grad_norm=0, mean_token_accuracy=0` at every step: the enriched system prompt is a median 3,016 tokens, so a 1024-token `keep_start` window is pure system prompt, which `response_only` masks to −100 → no valid targets → zero gradient (verified: 0 unmasked labels in `labels[:1024]` on 10/10 real conversations). Under `all_tokens` the same bug is **silent and flattering** — the near-identical system prompt across all 4,711 examples is trivially predictable, which is the likely explanation for eval_loss 0.17 / token-accuracy 0.9505 coexisting with a held-out composite of just 0.51–0.57. Confirmed three more ways: `checkpoint-1767/training_args.bin` records `max_length=1024, keep_start`; C0 log step 10 `num_tokens 8.192e4 / (10 × 8)` = exactly 1024.0/sample; C2 log step 50 `4.096e5 / (50 × 8)` = exactly 1024.0/sample. **Blast radius:** `sft-gemma4-v3` and `sft-gemma4-v4` both trained on 1024-token windows and **never saw a tool-calling turn** — those sit far past token 1024. So R14's C0/C1/C2 factorial was three names for one experiment (all collapsed to 1024), and **R15's "task-a-sft-v2 regresses" verdict, while internally valid (identical handicap both sides, contamination-controlled audit, statistics sound), does not establish that the corpus is bad** — it was measured between two models that never trained on the turns the corpus fix targets. The inference-side held-out harness is unaffected (own `--max-seq-length 8192`). **Fixed (2026-08-13, `c5906e7`):** `training/sft.py::_sft_length_kwargs()` passes the configured ceiling under whichever name the installed TRL accepts, with regression cover in `tests/unit/test_training.py::TestTrainerMaxLength` that builds a real `SFTConfig` and asserts `max_length == 8192`. Depends on `bac1d98`, which fixed `render_response_only_sample` returning 2 tokens / 0 unmasked labels (`apply_chat_template(tokenize=True)` returns a `BatchEncoding` on transformers 5.x; `list(mapping)` yielded its keys) — `response_only` had been broken for **every** model family since the transformers 5.x upgrade, so no C2 result has ever been valid. **Expect corrected runs to be much slower** (~4.6× the tokens/step plus superlinear attention; the 1024-token run was 3.65 s/it) and closer to the VRAM ceiling. Full analysis: `docs/sft_max_length_truncation_bug.md`. ~~**Do not start GRPO or re-derive `data/output/grpo/task_a` until a corrected SFT baseline exists.**~~ **Hold lifted 2026-08-16:** the corrected baseline exists — C2 `checkpoint-1767` at a verified `max_length=8192` (`training_args.bin` on all four C2 checkpoints), held-out composite 0.7595. Re-deriving `data/output/grpo/task_a` from the v2 corpus is now not just allowed but **required** before any GRPO run (R17).
- R17: **Cell C2 is the first valid Cat A SFT result and clears the pre-registered ≥0.75 bar — 0.7595 composite.** `checkpoints/sft_cat_a_c2/gemma-4-26B-A4B-it/checkpoint-1767` (`response_only` @ a real 8192 window, task-a-sft-v2 corpus; DVC stage `task_a_sft_gemma4_26b_a4b_c2`, recorded in `7e758da`). Scored on the pinned 206-row contamination-free set rebuilt via `scripts/build_heldout_clean_set.py` and confirmed by `--verify-against` to match the stored audit 206/206, so it is directly comparable to v3's 0.5709 and v4's 0.5120. Paired vs v3: composite **+0.1887, 95% CI [+0.1505, +0.2288]**, 83 rows better vs 6 worse, sign test **p = 2e-18**; state_acc +0.3447; tool_f1 +0.1416. **Real tool-calling ability** (the 71 rows requiring a call, not the silence-inflated aggregate): `tool_f1` **0.249 → 0.636**, "emits NO tool call" **57.7% → 12.7%**, exact call (name+args) **21.1% → 60.6%**, spurious calls on the 135 no-tool rows **8.2% → 1.5%**. Residual failures: 44/71 perfect, 18 right-name-wrong-args, 9 silent (2 "announce-but-don't-call"), **0 wrong/hallucinated tool names** — tool *selection* is solved, **argument fidelity is the new bottleneck**. Caveats: (a) C2 changed loss mask **and** window together, so the +0.1887 is not attributed between them — cell C1 (`all_tokens` @ 8192) would separate them but is an appendix, not a gate, since R14 measured that `all_tokens` spends ~71% of gradient on a near-identical system prompt and 2.5% training the model to *generate* tool results; (b) `task` stays 0.1942 and is near-meaningless per-turn (it scores reaching the terminal state on mid-conversation turns); (c) the audit loads with `load_in_4bit=True` against bf16 training, so 0.7595 is a floor for bf16 serving and the bar was cleared *under* that handicap. **Next step is GRPO on this checkpoint, blocked on three verified defects:** (1) `configs/training/grpo_cat_a.yaml:91` sets `data.source: "data/output/task_a"`, a directory that **does not exist** (only `grpo/`, `heldout/`, `sft/` live under `data/output/`), and `grpo.py:739` reads it directly → fails at data load; (2) `data/output/grpo/task_a` is v1-derived (2026-07-09 vs the v2 corpus's 2026-08-06), so its GT encodes the superseded convention and would penalise exactly what C2 learned — it feeds both training rows and the in-run reward-hacking detector (`_HeldOutEvalCallback`, `grpo.py:750`); regenerate with `scripts/filter_grpo_data.py`, whose defaults already read `data/output/sft/task_a_splits` and write `data/output/grpo/task_a`; (3) `grpo.py:775` derives `output_dir` from the config stem with no override and `scripts/run_phase2_grpo.sh:143` writes a fixed non-run-specific `PATCHED_CFG` — the exact R13 pair fixed for SFT and never ported, costing per-run provenance on a ~9 h run (R9: Gemma-4 gets HF `generate()` rollouts at ~31 s/step, ~8.6 h per 1000 steps). Full analysis, samples and repro: `docs/cat_a_c2_heldout_result.md`; tables via `scripts/stratify_heldout_audit.py`, which reproduces R15 §1/§3 exactly against the stored v3/v4 audits. **All three blockers cleared 2026-08-16:** (1) `grpo_cat_a.yaml` repointed to `data/output/grpo/task_a`; (2) that directory regenerated from the v2 corpus (train 2,502 → 2,558, validation 290 → 289) and **verified to carry the v2 convention** — `P(tool call | advancing turn)` is exactly **0.0000** (25,311 advancing turns, 0 with tool calls) against the old data's **0.3555**, with `P(tool call | self-loop)` 0.398 → 0.813; `dvc commit`+`push` done, remote in sync; (3) `_resolve_output_dir()` ported to `grpo.py` (regression cover: `tests/unit/test_grpo_output_dir.py`, 7 tests) and `run_phase2_grpo.sh` now writes a `RUN_TS`-stamped patched config plus `frozen_grpo_config_<RUN_TS>.yaml` beside `train.log`, with the checkpoint path pinned via an explicit `output_dir` — verified by dry-run: two invocations produce distinct patched configs and both resolve to the stable `checkpoints/grpo_cat_a/gemma-4-26B-A4B-it/`. **Unrelated pre-existing breakage to know about before enabling trajectory mode:** `tests/unit/test_trajectory_rollout.py` has 5 failures on `transformers` 5.x (`TypeError: int() argument must be … not 'tokenizers.Encoding'` at `trajectory_rollout.py:254`) — the same API-drift class as `bac1d98`. It does **not** block the default GRPO path (`grpo_cat_a.yaml` uses `reward_business_logic` with no `rollout:` section, so `make_replay_rollout_func` is never reached), but `reward_business_logic_trajectory` / trajectory rollout is broken until fixed. **Fixed 2026-08-16 (`a9685bd`)** — see R18.
- R18: **Cat A GRPO cannot learn: the reward has no resolution. Do not start a 1000-step run in either rollout mode.** The optimizer is healthy — across the 50-step diagnostic on the C2 base, `grad_norm` 0.008–0.392 against a kill line of 50 and `kl` 0.62–1.94 against 10, so the `scale_rewards: none` / `dr_grpo` / `max_grad_norm: 0.2` stabilization works. But every step logged `reward 1.0, reward_std 0, frac_reward_zero_std 1`: GRPO derives advantages from **within-group** variance, and a tied group gives exactly zero gradient. **The reward function is not the bug** — scored directly it returns 1.0 for a GT-perfect completion, 0.0 for a bogus state + hallucinated tool, 0.44 for prose with no annotation. The prompt distribution saturates it: **63.9%** of the 27,056 turn rows carry no GT tool call, `tool_call_f1([], []) == 1.0` gives those 0.40 free, and C2's 0.9369 state accuracy usually collects the other 0.40 — so ~59% of prompts score exactly 1.0. On 206 real completions the per-turn reward takes just **11 distinct values, 81.1% of them exactly 1.0**. **Three independent fixes each moved the needle and none sufficed:** (1) the tool-bearing mix (`fec93f1`, `data.tool_bearing_ratio`) raises the informative-prompt share only 19.4% → 23.5% (ratio 0.5) → 29.3% (0.7) → 38.0% (pure filter), because the graded reward saturates on tool-bearing rows too (mean 0.886 vs a strict held-out `tool_f1` of 0.636); (2) **trajectory mode fails outright** — wired in `65cf697`, at step 5 **8 of 8 genuinely distinct trajectories (lengths 77–113) all scored exactly the same reward**, which rules out both "policy too deterministic" and "the per-turn lattice is the cause", and contradicts `reward_business_logic_trajectory`'s docstring claim that aggregating over T turns restores variance; (3) temperature 0.8 → 1.4 does move `frac_reward_zero_std` 1 → 0, confirming the rollouts were varying *lexically* not *behaviourally*, but only to `reward_std` 0.00221–0.02283 — at or below the diagnostic's own 0.02 "under-resolves" threshold, and buying it degrades the policy. **Recommended path: preference learning (DPO/ORPO) on contrastive pairs**, where every pair carries a guaranteed margin and no reward variance is needed — chosen = gold turn, rejected = the failures C2 actually makes (announce-but-don't-call; self-loop/advance flips; right tool + wrong args, 18 of 71 tool-bearing rows). Two constraints: mine negatives from the model's own errors rather than only synthetic corruptions (R15 showed a structurally uniform edit gets learned as an unconditional habit), and **never source negatives from the held-out audit set** — exclude by `user_turn_fingerprint` (`data/heldout_clean_set.py`), or the only contamination-free measuring stick for this lineage is destroyed. Do **not** inject edited completions into GRPO groups: GRPO weights by logprobs under the current policy, so off-policy edits bias the gradient. **Defects fixed en route:** (a) trajectory gold scripts are keyed by `prompt_key`, and 76 keys collided (138 rows, 5.4%, one key 8 ways) so those rows replayed a *different* conversation's script — mis-scoring that depresses reward and therefore *inflates* variance, which would have biased this very experiment toward a false pass; now deduped; (b) `normalize_chat_template_ids` in `training/_utils.py` now shares the transformers-5.x `BatchEncoding` unwrap between `sft.py` and `trajectory_rollout.py` — the drift broke them silently and loudly respectively, and recurred only because `bac1d98` fixed it in place instead of sharing it (suite 1391 passed/5 failed → **1433 passed/0 failed**); (c) **the R5 reward-hacking guardrail has been silently inactive on every Gemma-4 GRPO run** — every held-out eval died with `grpo_heldout_eval_failed — num_kv_shared_layers is 0 …` while training carried on, because `_unwrap_unsloth_gemma4_kv_zero_proxy()` ran *before* `FastLanguageModel.from_pretrained`, which reinstalls the proxy; now re-armed after load with an explicit `generation_config` at both `generate()` sites, **but not yet independently confirmed to run — verify before relying on it**; (d) TRL 1.0.0 dropped `max_prompt_length`, now filtered with each dropped key named in the log (the 7,680 bound was non-binding: prompts are median 4,408 / p90 6,109 / max 7,738). Full analysis, per-step tables and repro: `docs/grpo_reward_resolution_investigation.md`. **Update (2026-08-17): the recommended path is built.** `training/dpo.py` (one entry point for both DPO and ORPO, selected via `dpo.method`) plus `scripts/run_phase2_dpo.sh` now exist, consuming contrastive pairs from `scripts/build_preference_pairs.py` (synthetic corruptions of gold turns) and `scripts/mine_model_negatives.py` (on-distribution negatives mined from C2's own greedy errors). The first mining run found C2 wrong on only 51/399 TRAIN prompts (12.8%) — far below the 38.0% the held-out audit measured on TEST — with two untested explanations: a train/test split effect, or `mine_model_negatives.py`'s `_classify()` being more lenient than the audit's composite scorer. `scripts/investigate_mining_yield.py` decomposes the two; its classifier-effect probe (no GPU needed) already ran for real against the stored held-out audit and found `_classify()` **stricter**, not looser, than the composite (39.4% vs 38.0% on the identical 71 tool-bearing rows) — **the classifier-effect hypothesis is refuted**, leaving the split effect as the sole open explanation. Mining from `--split validation` would test that directly and is built + unit-tested, but **not activated** — `dvc.yaml`'s `task_a_preference_model_negatives` stage still runs `--split train` — pending (a) a real GPU run of the split-effect probe, and (b) two documented-but-unresolved gaps: flipping the mining split would contaminate DPO's own eval split (`data.validation_source` in `configs/training/dpo_cat_a.yaml` is built from the same corpus split the mining/guardrail partition covers, and only the guardrail is protected, not the eval file), and the mining CLI's `--guardrail-reserved-fraction`/`-seed` flags must be kept in sync by hand with `dpo_cat_a.yaml`'s matching config keys (`reserve_guardrail_slice` in `src/llm_workflow_agents/data/heldout_clean_set.py` — nothing enforces agreement). Full design, risks and rollout: `docs/superpowers/specs/2026-08-17-mining-yield-investigation-design.md`.
- R19: **Cat A DPO OOMs on the training step after every R5 held-out guardrail eval. The guardrail is the cause; HF Trainer's own periodic evaluation is not.** Bisected 2026-08-18 across 15 smoke runs (`checkpoints/dpo_cat_a_smoke*/`, all from the C2 checkpoint `checkpoints/sft_cat_a_c2/gemma-4-26B-A4B-it/checkpoint-1767`). Two arms at `max_seq_length: 5120`, same fixture, 6 steps: **guardrail on / trainer eval off → OOM at step 4** (smoke14); **guardrail off / trainer eval on → 6/6 steps, 160.6 s** (smoke15). The failure is always the training step *following* a guardrail eval, never the eval itself. `fed2ea5`'s `torch.cuda.empty_cache()` in the callback's `finally` block recovered headroom without clearing the wall. **Settled non-levers:** `precompute_ref_log_probs: true` is required; `use_liger_kernel` is unusable (`NotImplementedError: Liger DPO loss is not implemented for PEFT models` — every Cat A DPO run is LoRA-on-C2); the length cap is **not** the wall (5120 and 4096 fail identically); gradient checkpointing is **already on** via Unsloth's `from_pretrained` default `use_gradient_checkpointing="unsloth"`, which looks unset only because `dpo.py` skips `get_peft_model`. Steady state is ~78 GiB of 79.18 GiB, so headroom is ~1 GiB and the shortfall magnitude (52/88/276/350 MiB across runs) reports whichever allocation failed first, not the size of the retention. **Leading hypothesis, untested:** the guardrail calls `model.generate()` (`dpo.py:686-695`) and nothing in `training/` ever saves or restores `use_cache` (`grep -n use_cache src/llm_workflow_agents/training/*.py` → none). HF Trainer sets `config.use_cache = False` at train start under gradient checkpointing; `generate()` turns it back on and leaves it on, so every subsequent step allocates a KV cache it previously did not — which matches all three observations (failure in the step *after*, `empty_cache()` not helping, and the trainer's forward-only eval being harmless). Test by save/force-False/restore around the eval, then re-run smoke14's config. **The `use_cache` hypothesis is REFUTED, and the real cause is found (2026-08-28).** The fix (`_build_heldout_callback` snapshots `model.config.use_cache` before the eval and restores it in the `finally` block; cover: `tests/unit/test_dpo_heldout_guardrail.py::test_evaluate_restores_use_cache_after_generate_flips_it`) is in the running code and did **not** clear the wall. smoke16 reproduced the failure exactly — OOM at step 4, the step after the step-3 guardrail eval, **350 MiB short**, the same shortfall smoke14 reported — and did so on an H100 NVL with **93.09 GiB, ~14 GiB more headroom than the entire ladder had**. A fixed retention would have failed later or not at all. Keep the save/restore (it is independently correct), but it is not the answer. **The actual ceiling: `load_in_4bit: true` quantizes only 0.77 GiB of this model.** Measured by loading the C2 checkpoint exactly as `train_dpo` does: **411 `Linear4bit`** modules against **631 `Linear`** + **189 `Gemma4ClippableLinear`**, and **45.83 GiB of parameters** still resident (`memory_allocated` 45.89 GiB right after load). The MoE experts are fused 3-D `nn.Parameter` tensors — `experts.gate_up_proj` is shape `(128, 1408, 2816)`, 28.36 GiB across layers, plus `experts.down_proj` at 14.18 GiB — and bitsandbytes quantizes by **swapping `nn.Linear` modules**, so it never sees them. `quantization_config` is present and honoured; it simply reaches 5.5% of the weight mass. **So the run carries ~45.8 GiB of weights where the config implies ~13 GiB — ~32 GiB of unintended overhead, and the "~1 GiB headroom" was structural, not a leak.** This also explains the ladder's most confusing datum, that 4096 and 5120 "fail identically" (halving the cap moves the `[2, S, 262144]` logits tensor ~4 GiB against a 32 GiB overshoot — the cap was never the lever). The R19 bisect stands: the guardrail is the **trigger**, not the cause. **No drop-in fix** — the Hub has no bitsandbytes NF4 build of this model (only GGUF / AWQ / GPTQ / MLX / FP8, none trainable through Unsloth+PEFT), which is also why Unsloth's mapper points at the bf16 mirror rather than a `-bnb-4bit` repo. **The fix is to attack peak, not retention, and it is now built (2026-08-28).** `scripts/run_phase2_dpo.sh --chunk-steps N` trains in chunks; each chunk is its own process, so the GPU empties between training and scoring and only one model copy is ever resident. A *concurrent* subprocess was rejected — it needs its own ~46 GiB while training holds ~46 GiB, and this machine has one GPU. Scoring runs as a separate `scripts/heldout_composite_audit.py` process, and `scripts/dpo_guardrail_decide.py` reads `trainer_state.json` plus the audit JSONs and reuses the existing `is_reward_hacking` so the two guardrail paths cannot drift (it exits 2 rather than "continue" when it cannot read its input — the R18(c) silent-guardrail shape). Verified end to end on a 6-step / 3-step-chunk smoke: chunk 1 trained fresh to step 3, chunk 2 resumed from `checkpoint-3` to step 6, both scored, GPU measured at 1 MiB between phases, and **step 4 completed** — the step that failed in smoke7-9, 13, 14 and 16. Two limits: the live run exercised only the *continue* path (the *stop* path has unit cover only), and the audit script samples the whole validation split with a fixed seed rather than the reserved guardrail slice the in-process callback used — harmless while mining runs on `--split train`, but revisit if mining moves to validation. `--chunk-steps` must equal `dpo.save_steps`, and the runner fails early if it does not. Also open as a deliberate decision, not a default: `load_in_4bit=True` now buys ~0.8 GiB while imposing a 4-bit base under a bf16-trained adapter (the mismatch R17 flags for the audit); turning it off costs ~2 GiB and removes it. **Blocking environment defect found en route:** a `.venv-train` rebuilt without `scripts/install_train.sh` completing has torch/transformers 5.12.1/peft/datasets but **no `trl`**, and `include-system-site-packages = true` + `_opt_venv.pth` then resolve `trl` to the base image's **0.23.1**, whose `DPOTrainer` selects its row-processing path from the *model* type and dereferences `processing_class.tokenizer` unconditionally (`trl/trainer/dpo_trainer.py:739`) — Gemma-4 takes that vision path, Unsloth supplies a plain tokenizer, and the run dies with `AttributeError: TokenizersBackend has no attribute tokenizer` **after** the 26B load. Fix is `VIRTUAL_ENV=.venv-train uv pip install --no-deps "trl==1.0.0"` (verbatim `install_train.sh:62`) **plus `rm -rf unsloth_compiled_cache/`**, which inlines TRL's source. Do NOT instead fall back to `/opt/venv` alone: transformers 4.57.6 **cannot load Gemma-4 at all**. Guarded by `training/dpo.py::_assert_dpo_row_processing_support`, called from `_resolve_trl_classes` before the 26B load (cover: `tests/unit/test_dpo_method_availability.py`). The smoke config is now tracked as `configs/training/dpo_cat_a_smoke.yaml` so it survives a rebuild, and note the HF cache is per-repo: pre-fetch `unsloth/gemma-4-26b-a4b-it`, not the google repo, or you download 51.6 GB the run will not use (weights are byte-identical; only `config.json` / `tokenizer_config.json` differ). **Container confound:** the rebuilt image sets `UNSLOTH_VLLM_STANDBY=1` in `/etc/environment`, and Unsloth **removes** the `PYTORCH_ALLOC_CONF=expandable_segments:True` that `scripts/run_phase2_dpo.sh:86` exports — so the runner's fragmentation mitigation is silently discarded, and pre-/post-rebuild runs are not comparable. Launch with `env -u UNSLOTH_VLLM_STANDBY` (DPO does no vLLM rollouts, so standby buys nothing). Unsetting it narrowed one OOM from 276 MiB to 88 MiB — real but not sufficient. **The production config is now established, and it needed three fixes (2026-08-28).** (1) **Cap 8192 does not fit for TRAINING** — probed for 2 steps, it OOMs in `loss.backward()` asking 7.80 GiB with 5.80 GiB free (87.26 GiB in use) on the 93 GiB H100 NVL. Precompute survives because it is forward-only. **6144 holds** (2/2 steps probed). (2) **The pair files must be length-filtered to the cap.** TRL's collator truncates `prompt + completion` to `max_length`, and `chosen`/`rejected` differ only in the trailing assistant turn, so a truncated pair collapses to identical token ids — completion_mask all zeros, margin exactly 0, gradient exactly 0, silently. Measured at 6144: unfiltered rows give `entropy: nan` and grad_norm 137; filtered rows give entropy 0.049 and grad_norm 32. Build with `scripts/build_dpo_smoke_fixtures.py --cap 6144 --n-train 5000 --n-validation 500 --n-model-negatives 51 --out-dir data/output/preference/task_a/cap6144` (kept 5,000 of 5,447 train scanned, 500 of 538 validation, all 51 mined negatives). (3) **`precompute_ref_log_probs` also walks the EVAL split**, which `max_train_rows` does not cap; the filtered 500-row validation file fixes that too (the full file is 3,450 rows). **Host-memory defect found the same day and unrelated to this project:** `/dev/shm` is tmpfs and held **61 GB** of stale `flashtalk_models` / `flashtalk-sol-env` caches from another user, dated three weeks earlier, mapped by no process — so every run started with 154 GB available instead of 215 GB. Stacked on the ~60 GB spike that `datasets.map(num_proc=16)` produces during tokenizing (measured: 22 processes, 59.5 GB tree RSS), swap filled and the first 500-step attempt slowed from 1 s/row to 587 s/row before the user killed it. Removing those files restored 61 GB and drained swap to zero. Check `du -sh /dev/shm/*` before blaming the trainer. **The 500-step run is blocked by a host-memory leak in the precompute (2026-08-29).** Two attempts were killed by the kernel OOM killer, both during `precompute_ref_log_probs`, neither during training. Measured by sampling `VmRSS` against the precompute's own row counter: **64 MB per row**, linear across 110 consecutive rows (row 2,383 -> 2,493, RSS 154.1 -> 161.0 GB), projecting to ~320 GB over the 5,000-row split on a 216 GB machine. It explains everything: the kill point **moved with available memory** (row 2,222 with 61 GB of `/dev/shm` occupied, row 3,277 after freeing it — a fixed allocation would fail at the same row), rows degraded from 1.6/s to 279 s before each kill as the kernel swapped, and `Killed` with no traceback is SIGKILL so no Python error is ever written. `DPOTrainer._precompute_ref_logps` appends only two scalars per batch (~40 KB total), so the leak is in the data path around that loop, not the accumulator — reading it and declaring the precompute innocent was a mistake. Suggestive but unverified: 64 MB is close to a completion-length logits slice at this vocabulary (128 x 262,144 x 2 bytes = 67 MB). **First hypothesis:** `dpo.py` never sets `dataloader_pin_memory`, so HF's default `True` applies and pinned host blocks are cached by size and never returned; one line to falsify. **Also fixed en route, and NOT the cure:** `/dev/shm` is tmpfs and held **61 GB** of stale `flashtalk_*` model caches from another user, mapped by no process, so every run began with 154 GB available instead of 215 GB; removing them restored 61 GB and drained swap, but the leak continued — check `du -sh /dev/shm/*` before blaming the trainer, and do not stop there. **Tooling:** sample an explicit PID and use `nvidia-smi --query-compute-apps` for liveness — three samplers falsely reported "process gone" against a healthy run (started before the process existed; a `pgrep -f` pattern containing `(`, an ERE metacharacter, which silently matches nothing; and a pattern that matched the checking shell). Give any such sampler a `MemAvailable` floor that kills the run, so a failure costs a diagnostic instead of the machine. Full evidence: `docs/dpo_memory_ceiling_investigation.md` section 10. **FOUND AND FIXED 2026-08-29.** The owner is Unsloth's global `CPU_BUFFERS` pool: `Unsloth_Offloaded_Gradient_Checkpointer.forward` copies each layer's hidden states to host memory (`unsloth_zoo/gradient_checkpointing.py:162`) and keeps them in a module-level list. Censused live inside a real run it grew **200 buffers / 4.36 GB -> 735 / 33.49 GB** across eight samples, matching the retained hidden-state tensors **to the decimal** at every sample; buffers are both resized and appended (a new one whenever none fits the current sequence length), so 5,000 rows of varying lengths bound nothing. **The obvious fix does not work:** Unsloth installs the offload by replacing `torch.utils.checkpoint.CheckpointFunction`, *below* the per-module `gradient_checkpointing` flag — pausing that flag measured 65.3 MB/row, unchanged. The same blind spot makes TRL's own guard ineffective: `compute_ref_log_probs` already does `with torch.no_grad(), disable_gradient_checkpointing(...)` and Unsloth's compiled copy carries it verbatim, but neither reaches the patched CheckpointFunction. **Fix:** `training/dpo.py::_gradient_checkpointing_paused` calls `unpatch_unsloth_smart_gradient_checkpointing()` on entry and restores it in the `finally` block, wrapping the trainer construction where the precompute runs (cover: `tests/unit/test_dpo_precompute_checkpointing.py`, 10 tests; three fail against the flag-only version so it cannot return). **Measured on the real runner: 65.00 MB/row -> 0.00 MB/row, RSS flat at 3.0 GB from row 104 to row 922.** Three hypotheses were refuted by measurement first — pinned host buffers, W&B (that correlation was confounded: every `--no-wandb` run was also a short run), and the module flag. `scripts/diagnose_precompute_leak.py` settled it; note its hand-rebuilt loop does **not** leak at any scale, which is what pointed at the caller, so use its `--real` mode, which censuses from inside the actual `train_dpo` call. Fixtures must be rebuilt first via `scripts/build_dpo_smoke_fixtures.py` (committed in `8cf2b93`; the originals lived in a per-session scratchpad the container rebuild deleted) — rebuilt 2026-08-28 to 64 train / 16 validation / 8 model-negative rows under cap 5120. Full ladder, evidence, hypothesis and ordered next steps: `docs/dpo_memory_ceiling_investigation.md`.
- R20: **The voice modality now exists in Task A generation.** `generate_workflow_dataset`'s `modality_preset` (`"default"` all-text, `"voice_mix"` 70/30, `"voice_heavy"` 40/60, `"voice_only"` all-voice — `MODALITY_PRESETS` in `generate_workflows.py`) draws each conversation's modality; `barge_in_rate` is the share of VOICE conversations for which the teacher model is **asked** for one `<unspoken>` interruption — it is a request, not a guarantee. Whether one was written is recorded separately: `apply_barge_in_loss_flag` (`voice_convention.py`) runs after every teacher attempt, repair retry and placeholder fallback have settled and sets the sample's `barge_in` from what the messages actually contain, so the flag is a fact about the row rather than a label that can disagree with it (the R12 shape). The sidecar carries both `barge_in_requested` and `barge_in_realized`; the gap between them is "the teacher was asked and did not deliver", which `scripts/check_voice_batch.py` reports as a warning. The placeholder generator can now also realise a barge-in, deterministically — a fallback row inside an otherwise teacher-driven run can realise one. **It may only interrupt a SELF-LOOP turn** (`[STATE: X → X]`): the recovery turn repeats the interrupted turn's `from` state and `find_barge_in_violations` requires exactly that, so interrupting an advancing turn `[X → Y]` wrote a recovery `[X → X]` into a conversation already sitting in `Y` and left `ground_truth.state_sequence` discontinuous — measured at 40/40 barge-in rows before the fix, and invisible to every gate because the checker's rule and the defect agreed. The restriction costs realisation rate: under the tool-call stay convention almost every self-loop turn is a *silent* tool-call turn with no speech to interrupt, so only **11/200** placeholder voice rows realise an interruption at `barge_in_rate=1.0` (40 per level, seed 777), against 198/200 before — roughly 1.4% at the default rate of 0.25. (An earlier draft of this entry said 22/200; that was the count of candidate *turns*, two per row, not rows.) Selecting no candidate logs `placeholder_barge_in_no_candidate` rather than passing silently. If a higher rate is wanted, the change to make is annotating the recovery from the interrupted turn's **`to`** state and tightening `find_barge_in_violations` to "the recovery is a self-loop at the interrupted turn's `to`" — that is correct for both advancing and self-loop turns, and is a decision nobody has taken yet, not an oversight. Chunk length is measured over *spoken* characters: `<unspoken>` is the one marker that legally sits inside a chunk and is never spoken. `scripts/check_voice_batch.py` therefore counts only rows whose `generation_source` is `"teacher"` toward the teacher's realised total; the sidecar's aggregate `barge_in_realized` counts every row regardless of source and can no longer distinguish a real teacher delivery from a degraded run the placeholder covered for. **The enforced format contract lives in `src/llm_workflow_agents/data/voice_convention.py`** — `find_voice_violations` is what decides whether a row enters the corpus, so that module, not the prose anywhere else, is the authority on what the corpus contains. **The contract is stated once in prose too, in `render_voice_format_rules()` (V1–V6) in `voice_convention.py`, and both prompts render from that one string** — the teacher prompt (`_teacher_system_prompt`) and the served/trained system prompt (`build_enriched_system_prompt`). `_VOICE_RULES` no longer exists; do not re-create a per-prompt copy. The prose and the checker still live side by side in the same module and are deliberately not collapsed into each other: the checker is executable logic for a repair loop and the prose is an instruction addressed to an LLM, complete with a worked example. **One hand-maintained copy remains — `_RICH_VOICE_OVERRIDE` in `generate_workflows.py`** — and it stays separate on purpose: it instructs the teacher on *authoring a system prompt*, which is a different instruction, and it has its own tests (`tests/unit/test_voice_prompt_contract.py`). What *is* shared is imported — `CHUNK_TARGET_CHARS`, `CHUNK_MAX_CHARS`, `TURN_TARGET_CHUNKS`, `TURN_MAX_CHUNKS` and `ACKNOWLEDGEMENTS` are rendered into the prompts from the module, and `tests/unit/test_voice_prompt_contract.py` fails if a prompt copy stops naming a marker the checker enforces. Drift in the prose is self-correcting in one direction only — a prompt that under-states a rule produces rows the checker rejects and the repair loop retries, at API cost; a checker that under-states a rule admits bad rows silently. Edit the checker first. **The `all_tokens` SFT loss recipe cannot honour the per-message `loss` flag** — it has no per-token masking mechanism, only TRL's own collator training every non-pad token uniformly — so a voice row's `loss:false` turn (the model's own barge-in `<unspoken>` marker, replayed into its past turns) would become a live training target under `all_tokens`, teaching the model to emit the marker. `response_only` (`render_response_only_sample`) is required for any corpus containing voice data; `sft.py` detects `loss:false` messages in the source corpus and logs `all_tokens_ignores_loss_flag` if `all_tokens` is still configured. **That protection was unreachable when first written and is reachable now** — every consumer of the key existed and nothing wrote it, so the warning could never fire and read as "corpus is clean" (the R18c shape). `apply_barge_in_loss_flag` is now the single producer: it sets `loss: false` on the assistant turn carrying `<unspoken>` and deletes any stale `loss` key from every other message, so a conversation with no marker carries no `loss` key at all and the absent-key default (`True`) governs it. A teacher-hallucinated `loss` key therefore cannot silently drop a real training target. **The held-out audit must report text and voice separately and never blend them** — `heldout_composite_audit.py` scores `voice_format_compliance` as a standalone guardrail metric over voice rows only, never folded into the composite, **and reports the composite and its three components per modality** (`summary.modalities`). The aggregate `mean_composite` is kept so stored audits stay readable, but on a mixed sample the JSON carries `mixed_modality: true` and the printout labels it `MIXED-MODALITY` — it is not comparable to 0.7595 and quoting it against the 0.75 bar moves that bar without a decision. `--modality {text,voice,all}` (default `all`, which is the unchanged sampling path) gives a clean single-modality sample. The pinned 206-row held-out set (`build_heldout_clean_set.py`, `--modality` default `"all"`) that produced R17's 0.7595 figure was built text-only, before voice existed; blending voice rows into that set or into a future comparable run would break the only link back to that number. **`scripts/split_task_a_sft.py` shuffles per modality, not over the concatenated pool** — a positional shuffle redraws every assignment when the pool grows, and merging the 2,400 voice rows moved 7 of those 278 candidates into train (measured, at real sizes). Grouping by modality makes an additive batch additive: the text group is the same list, the same size and the same seed, so its assignment is byte-identical (verified — re-splitting today's corpus reproduces `task_a_splits` exactly). The guarantee's honest limit is that adding rows to an **existing** modality group still reshuffles that group, so after any merge run the splitter with `--assert-unmoved data/output/sft/task_a_splits` against the prior splits; it matches by `user_turn_fingerprint` (ids are not unique) and exits non-zero on any row that changed split. Then rebuild the pinned set and check it still verifies: `python scripts/build_heldout_clean_set.py --candidate-split data/output/sft/task_a_splits/test.jsonl --exclusion-split <v1 train> --exclusion-split <v1 validation> --expect-clean 206 --verify-against runs/audit/heldout_ckpt1767_v2corpus.json`. **Before spending the API budget, run a live 15-conversation smoke** (`./scripts/generate_voice_data.sh --smoke-test --teacher-model <model> --output-dir /tmp/voice_smoke`): it is the only pre-flight signal that the teacher can hit the voice format at all, and `scripts/check_voice_batch.py` runs at the end of every generation to fail a batch that is mostly placeholder rows (spec risk 2). Note that `task_a_sft_clean` now hard-depends on `data/output/sft/task_a_voice`, which does not exist until that run happens, so the text chain cannot be `dvc repro`'d before it. **Latent hazard found while wiring the corpus gate for this feature (2026-08-20):** one raw Task A conversation, `L4_061_6` (`data/output/sft/task_a/l4_merged_20260630.jsonl`), has a literal `[END_CONVERSATION]` in its **system** message (dated seven weeks before the voice modality existed — unrelated to this work). Harmless today: `find_voice_violations`' only production caller (`_find_violations`) never sees a system turn, because `generate_workflows.py:1583` strips any system message the teacher emits before the coherence-repair loop runs, and the generator inserts its own system message afterward. But if `find_voice_violations` is ever pointed at a fully-assembled conversation *including* the system turn, this row will be rejected. Named here so the next person finds it in seconds instead of rediscovering it from a failing gate. A newly-authored system prompt cannot repeat it: the rich prompt is generated *after* the repair loop and so is never checked, so `_scrub_rich_prompt` deletes `[END_CONVERSATION]`, `<unspoken>` and `[TRANSFER]` from it in both modalities (`_RICH_PROMPT_FORBIDDEN`) rather than trusting the instruction not to write one. `<S>` is deliberately allowed in a *voice* system prompt — `_RICH_VOICE_OVERRIDE` asks for chunked dialogue lines — and forbidden in a text one. **The served/trained system prompt now switches on modality.** `build_enriched_system_prompt` (`data/system_prompt.py`) appends a voice-format block whenever a sample's `modality` is `"voice"`; a text sample (`modality` absent or `"text"`) renders byte-identical to before, guarded by the committed fixture `tests/fixtures/text_prompt_baseline.json` and `tests/unit/test_system_prompt_voice.py::test_real_text_rows_render_byte_identically`. Four call sites inherit the switch because they all route through this one function: the Phase 1 benchmark (`eval/agent_benchmark.py`), the held-out audit, GRPO rollouts (`training/grpo.py`), and SFT training (`training/sft.py`). The voice rules themselves live in exactly one place, `render_voice_format_rules()` (`data/voice_convention.py`); both the teacher prompt and this served/trained prompt render from it, so the two cannot drift on what the format contract says (they can still drift on *whether* a row was generated to honour it — see the barge-in-realised note above). Every caller inherits the switch, because they all route through this one function — the four named above plus the web UI and several scripts. Do not trust a count written here: it goes stale the moment someone adds a caller, and it already has twice. Get the current list with `grep -rln build_enriched_system_prompt --include='*.py' src/ scripts/`. Note the held-out audit is not a direct caller; it inherits through `grpo._load_grpo_jsonl`. **Phase 1 quality is a blend on any run that holds both strata, and a single stratum's score otherwise.** `blend_modality_scores` (`eval/composite_score.py`) combines a text-stratum score and a voice-stratum score as `DEFAULT_VOICE_WEIGHT * voice + (1 - DEFAULT_VOICE_WEIGHT) * text`, `DEFAULT_VOICE_WEIGHT = 0.30`; with either stratum absent it returns the other stratum's score unchanged, by float identity, not by an arithmetic shortcut that merely rounds to the same value — so merging this cannot move the existing ranking by itself. **A run holds both strata only when it is told to.** The two corpora are sibling directories and the loader globs one level deep, so `--data` is repeatable and both must be named: `--data data/output/benchmark/task_a --data data/output/benchmark/task_a_voice` (paths are sorted and de-duplicated, so flag order cannot change a result, and `--max-samples` caps each path so a smoke run cannot truncate one stratum to zero). The blend is written to `quality_summary.quality` in the result JSON; **`scripts/run_exp_a_per_level.sh` is the consumer that ranks on it**, falling back to `metrics.weighted_workflow_score` only for runs predating the summary. Never rank a two-stratum run on `metrics.weighted_workflow_score`: it is pooled over every row, so its effective voice weight comes from the row counts and drifts whenever the data is regenerated. A row whose `modality` is neither `"text"` nor `"voice"` is folded into the text stratum and named in `n_unknown_modality`/`unknown_modalities`, never dropped, so `n_text + n_voice == len(samples)` always holds. **The benchmark text stratum (`data/output/benchmark/task_a`, 258 conversations) is FROZEN** — the new voice stratum (`data/output/benchmark/task_a_voice`, `scripts/generate_benchmark_voice_data.sh`) is additive alongside it, never a replacement. **Blending changes what the Phase 1 composite measures, so it moves the ranking bar, not just the ranking.** A comparable ranking under the new composite needs Phase 1 re-run across the full 15-candidate set once a real voice stratum exists — that is a real, deliberate cost this branch does not pay, not a footnote to skip.
- R21: **Two pre-existing provenance defects found while scoping the voice benchmark work. Defect (1) is now FIXED; defect (2) stands.** (1) `dvc.yaml`'s `task_a_benchmark` stage description claims "1,000 placeholder conversations, no teacher model or API key required." The artifact actually on disk at `data/output/benchmark/task_a` holds 258 conversations, 250 of them teacher-generated by two different models (`gemini-3-flash-preview` for L1–L3, `gemini-3-5-flash` for L4–L5) — not placeholders, not 1,000. Running that stage as described would regenerate placeholder data and **delete the teacher-generated data behind the project's current Cat A ranking**. **Fixed 2026-08-24 by freezing the stage.** The artifact was ruled authoritative: it is the data behind the current ranking, so it must not change. `task_a_benchmark` now carries `frozen: true`, which makes `dvc repro` skip it, and its `desc` states what the files actually are — 258 conversations in 17 files, two teacher models, plus one hand-added `l3_insurance_premium_payment_th_*.jsonl` that does not follow the generator's naming convention. The `cmd` is retained for provenance only and does NOT describe how those files were made. **Note the urgency this had acquired:** `src/llm_workflow_agents/data/generate_workflows.py` is a declared dependency of the stage, and the voice branches changed it in four commits, so DVC considered the stage out of date — `dvc repro` would have fired it rather than skipped it. The defect was dormant for months and was armed by our own work. `tests/unit/test_dvc_benchmark_stage_frozen.py` guards the freeze, because unfreezing is a one-word edit whose cost is the corpus and nothing else would notice. **Still outstanding:** `dvc.lock` records `nfiles: 11` for this stage while 17 files are on disk, so the lock disagrees with reality. Freezing makes that harmless for `repro`, but it needs `dvc commit task_a_benchmark` on a machine with the DVC CLI (absent in this environment). Hashes must come from DVC; do not hand-edit `dvc.lock`. (2) `configs/benchmark/selection_weights.yaml` has **no code consumer** — nothing in `src/` or `scripts/` reads it (confirmed by grep). It states a Cat A quality formula (`0.5 * state_transition_accuracy + 0.5 * tool_call_f1`) that differs from the one `composite_score.py::compute_weighted_workflow_score` actually runs (`0.4 * state_transition_accuracy + 0.4 * tool_call_f1 + 0.2 * task_completion`). This is why `DEFAULT_VOICE_WEIGHT` (R20) lives in `composite_score.py` and not in this YAML file — adding it there would create a knob that does nothing.
