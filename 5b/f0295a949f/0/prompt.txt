Based on @scripts/run_exp_a_single.sh, generate a script to run concurrency benchmark to find maximum concurrent request that an LLM can process without degradation on single H100 GPU (80GB VRAM). Before implementing the script, let's write a comprehensive concurrency and latency analysis report in docx and put it in `docs` directory. This report should analyze performance of each category A (15B-30B) models based on its architecture (including FP8 quantized model) and limitation of targeted hardware (single H100) and vLLM schemes (KV cache quantization, GPU utilization).