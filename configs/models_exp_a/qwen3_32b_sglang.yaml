model:
  name: "Qwen/Qwen3-32B"
  family: "qwen"
  architecture: "dense_gqa_transformer"
  params_total: 32_000_000_000
  params_active: 32_000_000_000
  context_length: 131072
  precision: "bfloat16"
  vram_estimate_gb: 64

serving:
  engine: "sglang"
  tool_call_parser: "qwen"      # SGLang Qwen parser (vLLM equivalent: "hermes")
  # chat_template intentionally omitted: SGLang falls back to the HF tokenizer's
  # built-in chat_template (canonical for Qwen instruct models).
  mem_fraction_static: 0.90
  context_length: 8192
  max_running_requests: 256
  attention_backend: "triton"
  port: 8000
  kv_cache_dtype: "auto"

inference:
  temperature_deterministic: 0.0
  temperature_stochastic: 0.7
  stochastic_trials: 5
  max_tokens: 2048
