# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
.venv-train/
.venv-infer/
env/

# Unsloth on-disk JIT cache
unsloth_compiled_cache/

# Local run scratch (patched configs, etc.)
.runs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Data outputs — managed by DVC, not git
results/raw/

# Model checkpoints — subdirs tracked individually by DVC after each training run
# checkpoints/ itself is committed (contains .gitignore + .gitkeep)
*.safetensors
*.bin
*.pt
*.ckpt

# Logs
wandb/
*.log

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# GCP service account credentials (never commit)
*.json
# Track BiFrost gateway config explicitly (uses env.* placeholders, no secrets)
!deployments/**/config.json

# SQLite runtime state (BiFrost logs/config store) — machine-specific, may
# contain logged prompts/responses. Never commit.
*.db
*.db-shm
*.db-wal
